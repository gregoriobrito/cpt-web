## spring
spring:
  application:
    name: cpt-web
  datasource:
    url: jdbc:postgresql://localhost:5432/cpt
    driverClassName: org.postgresql.Driver
    username: cpt
    password: cpt
    hikari:
      maximum-pool-size: '3'

  jpa:
    show-sql: 'true'
    database-platform: org.hibernate.dialect.PostgreSQLDialect